# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Data / indexes / traces (generated)
data/index/
traces/
# Eval reports are generated, but keep two representative real-OpenAI runs in git
# so the README's numbers are reproducible artifacts (the Results section links them).
eval/results/*
!eval/results/eval-20260705T085510Z.json
!eval/results/eval-20260705T085510Z.md
!eval/results/compare-20260708T041312Z.json
!eval/results/compare-20260708T041312Z.md
*.npz

# OS / editor / assistant
.DS_Store
.idea/
.vscode/
.claude/

# pytest
.pytest_cache/

# ruff
.ruff_cache/

# Streamlit secrets (config.toml is committed; secrets are not)
.streamlit/secrets.toml

# Personal docs / study material (kept local, not published).
# Anchored to the repo root so it never matches the data/docs corpus.
/docs/
